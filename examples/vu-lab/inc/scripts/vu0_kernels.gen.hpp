// Generated by TyraX (the project's own VU0 kernels). Do not edit.
//
// A kernel is a FUNCTION, not a look: nothing installs it and
// nothing calls it unless your game does.
//
//   static Tyra::YourKernel k;
//   k.setParams(1.0F, 0.0F, 0.0F, 0.0F);
//   k.setTime(seconds);
//   k.run(in, out, count);   // Vec4 in, Vec4 out, BLOCKS
//
// run() blocks the EE and clobbers the COP2 register file the
// engine's own Vec4/M4x4 math uses, so nothing may have vector
// arithmetic in flight while it runs. There is no cross-element
// reduction on VU0 - a sum is something the EE does over the
// output array afterwards.
#pragma once

#include "scripts/vu0_script0_kernel.hpp"

namespace vukernel {

constexpr bool ENABLED = true;
constexpr int COUNT = 1;

// The kernels, in the order the generator found them.
constexpr int kRanges = 0;   // RangesKernel, batch 64

inline const char* name(int kernel) {
  static const char* const kNames[] = {"Ranges"};
  return (kernel >= 0 && kernel < COUNT) ? kNames[kernel] : "";
}

}  // namespace vukernel
