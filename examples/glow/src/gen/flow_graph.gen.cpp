// Generated by TyraX from the per-object Flow Graphs. Do not
// edit - regenerated on every build. Edit the graphs in the editor.
#include "scripts/script.hpp"
#include "scripts/sequences.gen.hpp"  // Play/Stop Sequence nodes
#include "scripts/flow_nodes.hpp"  // custom-node C++ bodies

#include <math.h>
#include <stdio.h>

#include <string>

namespace Glow {

// Scene "main": graph of "bloom-pulse" (object 24)
class FlowGraphScript_0_24 : public Script {
 public:
  void update(ScriptContext& ctx) override {
    if (ctx.scene != 0) return;
    if (ctx.sceneGeneration != generation) {
      // scene was (re)loaded - back to the initial state
      generation = ctx.sceneGeneration;
      frame = 0;
      started = false;
      delay3 = 0;
      every1 = 1;
    }
    frame++;
    if (delay3 > 0 && --delay3 == 0) {
      ctx.bloom = 192;
    }
    if (--every1 <= 0) {
      every1 = everyFrames(14.0F);
      ctx.bloom = 58;
      delay3 = everyFrames(7.0F);
    }
  }

 private:
  unsigned int generation = 0;
  int frame = 0;
  bool started = false;
  int delay3 = 0;
  int every1 = 1;
};

}  // namespace Glow

TYRA_SCRIPT(Glow::FlowGraphScript_0_24);
