name: showcase
volumes:
  tyra-game-volume:
  tyra-engine:
    name: tyra-engine-2c4a002b
services:
  compiler:
    environment:
      TERM: xterm-256color
    network_mode: host
    build:
      context: ./
      dockerfile: Dockerfile
    tty: true
    volumes:
      - tyra-game-volume:/src
      - tyra-engine:/tyra
      - ./:/host
      - "F:/Projects/tyra-editor/.claude/worktrees/menu-toggles-ui-customization-27a649/vendor/tyra:/engine-src:ro"
