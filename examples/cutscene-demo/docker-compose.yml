name: cutscene-demo
volumes:
  tyra-game-volume:
  tyra-engine:
    name: tyra-engine-0c33fc43
services:
  compiler:
    environment:
      TERM: xterm-256color
    network_mode: host
    build:
      context: ./
      dockerfile: Dockerfile
    tty: true
    volumes:
      - tyra-game-volume:/src
      - tyra-engine:/tyra
      - ./:/host
      - "F:/Projects/tyra-editor/.claude/worktrees/timeline-camera-entity-fea51d/vendor/tyra:/engine-src:ro"
