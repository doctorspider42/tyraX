name: cutscene-demo
volumes:
  tyra-game-volume:
  tyra-engine:
    name: tyra-engine-abf473e4
services:
  compiler:
    environment:
      TERM: xterm-256color
    network_mode: host
    build:
      context: ./
      dockerfile: Dockerfile
    tty: true
    volumes:
      - tyra-game-volume:/src
      - tyra-engine:/tyra
      - ./:/host
      - "D:/tyra-editor/.claude/worktrees/custom-screen-effects-9a26d4/vendor/tyra:/engine-src:ro"
