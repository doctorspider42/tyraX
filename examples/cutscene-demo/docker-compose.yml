name: cutscene-demo
volumes:
  tyra-game-volume:
  tyra-engine:
    name: tyra-engine-871bb1f1
services:
  compiler:
    environment:
      TERM: xterm-256color
    network_mode: host
    build:
      context: ./
      dockerfile: Dockerfile
    tty: true
    volumes:
      - tyra-game-volume:/src
      - tyra-engine:/tyra
      - ./:/host
      - "F:/Projects/tyra-editor/.claude/worktrees/nowy-task-fe01da/vendor/tyra:/engine-src:ro"
