# The authored controls overlay (res/ui) is checked in; built-in / baked
# assets (res/hud, res/menus panels) are regenerated by TyraX on
# every build.
/hud/
/menus/

# Baked static-model output (the .obj next to it is the source;
# regenerated on every build - docs/model-pipeline.md).
/models/*.tmdl
