name: script-demo
volumes:
  tyra-game-volume:
  tyra-engine:
    name: tyra-engine-d3652388
services:
  compiler:
    environment:
      TERM: xterm-256color
    network_mode: host
    build:
      context: ./
      dockerfile: Dockerfile
    tty: true
    volumes:
      - tyra-game-volume:/src
      - tyra-engine:/tyra
      - ./:/host
      - "F:/Projects/tyra-editor/.claude/worktrees/usable-highlighting-perf-a45d9a/vendor/tyra:/engine-src:ro"
