name: script-demo
volumes:
  tyra-game-volume:
  tyra-engine:
    name: tyra-engine-4d1b4a4a
services:
  compiler:
    environment:
      TERM: xterm-256color
    network_mode: host
    build:
      context: ./
      dockerfile: Dockerfile
    tty: true
    volumes:
      - tyra-game-volume:/src
      - tyra-engine:/tyra
      - ./:/host
      - "F:/Projects/tyra-editor/.claude/worktrees/sound-emitter-autoplay-bug-d4ef13/vendor/tyra:/engine-src:ro"
