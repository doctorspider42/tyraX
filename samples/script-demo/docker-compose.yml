name: script-demo
volumes:
  tyra-game-volume:
  tyra-engine:
    name: tyra-engine-shared
services:
  compiler:
    environment:
      TERM: xterm-256color
    network_mode: host
    build:
      context: ./
      dockerfile: Dockerfile
    tty: true
    volumes:
      - tyra-game-volume:/src
      - tyra-engine:/tyra
      - ./:/host
      - "F:/Projects/tyra-editor/.claude/worktrees/emulator-logs-debug-window-8b0d88/vendor/tyra:/engine-src:ro"
