name: showcase
volumes:
  tyra-game-volume:
  tyra-engine:
    name: tyra-engine-35c830b1
services:
  compiler:
    environment:
      TERM: xterm-256color
    network_mode: host
    build:
      context: ./
      dockerfile: Dockerfile
    tty: true
    volumes:
      - tyra-game-volume:/src
      - tyra-engine:/tyra
      - ./:/host
      - "D:/tyra-editor/.claude/worktrees/hubspot-calendar-sync-bug-b1cbcd/vendor/tyra:/engine-src:ro"
